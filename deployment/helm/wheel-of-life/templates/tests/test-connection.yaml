apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "wheel-of-life.fullname" . }}-test-connection"
  labels:
    {{- include "wheel-of-life.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "wheel-of-life.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
